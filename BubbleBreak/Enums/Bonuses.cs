namespace BubbleBreak
{
	public enum Bonuses
	{
		pos_time,
		double_score,
		pos_points,
		addition,
		subtraction,
		checkpoint,
		triple_score,
		next_in_seq,
		next_mf,			//TODO: chance_next_seq,  need to change the sprite file name to match enum
		bonus_mf,			//chance_bonus,
		tap_str,
		lucky_point_chance,	
		lucky_point_amount,	
		neg_time,
		neg_points,
		mystery,			// require multiple taps
		big_pos_time,		// require multiple taps
		big_pos_points,		// require multiple taps
		unused,
	}
}

